import styled from 'styled-components';

const Outer = styled.div`
  padding: 2rem 24px;
  margin-top: 2rem;
  background: #4e4e4e;
  color: #FFF;
  flex: 0 0 auto;
  display: flex;
  flex-direction: column;

  & a {
    color: #FFF;
  }
`;

export default Outer;
